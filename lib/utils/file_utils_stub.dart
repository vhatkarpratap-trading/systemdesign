void saveFileWeb(String content, String fileName) {
  throw UnimplementedError('saveFileWeb only works on Web');
}

Future<String?> pickFileWeb() {
  throw UnimplementedError('pickFileWeb only works on Web');
}
